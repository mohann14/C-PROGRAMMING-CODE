#include
int main() { 
int a,b,c;
scanf("%d %d %d",&a,&b,&c); 
if (a>b && a>c)
printf("A is biggest...\n") ;
else if (b>a && b>c)
printf("B is biggest...\n");
else
printf("C is biggest...\n"); return 0;
           }
